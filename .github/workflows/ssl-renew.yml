name: SSL Renew

on:
  workflow_dispatch:

env:
  APP_DIR: /opt/lqd
  WEBPHOTO_DIR: /opt/webphoto

jobs:
  ssl:
    name: "Renew SSL cert"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq sshpass > /dev/null
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.VPS_PORT }} -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts 2>/dev/null
          echo "SSHPASS=${{ secrets.VPS_PASSWORD }}" >> $GITHUB_ENV

      - name: Get SSL cert for lqd.bhquan.store
        run: |
          VPS="${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}"
          PORT="${{ secrets.VPS_PORT }}"

          # Tim ten Docker volume chua certbot data (WebPhoto dung certbot_data)
          echo "=== Find certbot volume ==="
          sshpass -e ssh -p $PORT $VPS "
            echo '--- Docker volumes ---'
            docker volume ls | grep -i cert
            echo ''
            echo '--- Nginx mounts ---'
            docker inspect shared-nginx --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{println}}{{end}}'
          "

          echo ""
          echo "=== Current cert in volume ==="
          sshpass -e ssh -p $PORT $VPS "
            docker exec shared-nginx ls -la /etc/letsencrypt/live/ 2>&1 || echo 'No cert dir'
            docker exec shared-nginx ls -la /etc/letsencrypt/live/lqd.bhquan.store/ 2>&1 || echo 'No lqd cert'
          "

          echo ""
          echo "=== Removing old/placeholder cert from volume ==="
          sshpass -e ssh -p $PORT $VPS "
            CERT_VOL=\$(docker inspect shared-nginx --format '{{range .Mounts}}{{if eq .Destination \"/etc/letsencrypt\"}}{{.Source}}{{end}}{{end}}')
            echo \"Cert volume/path: \$CERT_VOL\"
            docker run --rm -v \${CERT_VOL}:/etc/letsencrypt alpine sh -c '
              rm -rf /etc/letsencrypt/live/lqd.bhquan.store*
              rm -rf /etc/letsencrypt/archive/lqd.bhquan.store*
              rm -rf /etc/letsencrypt/renewal/lqd.bhquan.store*.conf
              echo Cleaned
            '
          "

          echo ""
          echo "=== Stopping nginx to free port 80 ==="
          sshpass -e ssh -p $PORT $VPS "docker stop shared-nginx 2>/dev/null || true"

          echo ""
          echo "=== Running certbot into Docker volume ==="
          sshpass -e ssh -p $PORT $VPS "
            CERT_VOL=\$(docker inspect shared-nginx --format '{{range .Mounts}}{{if eq .Destination \"/etc/letsencrypt\"}}{{.Name}}{{end}}{{end}}')
            echo \"Using volume: \$CERT_VOL\"

            docker run --rm \
              -v \${CERT_VOL}:/etc/letsencrypt \
              -p 80:80 \
              certbot/certbot certonly --standalone \
              --non-interactive \
              --cert-name lqd.bhquan.store \
              --email admin@bhquan.site \
              --agree-tos --no-eff-email \
              -d lqd.bhquan.store \
              2>&1
          "

          echo ""
          echo "=== Restarting nginx ==="
          sshpass -e ssh -p $PORT $VPS "
            docker start shared-nginx
            sleep 2
          "

          echo ""
          echo "=== Verify cert in nginx container ==="
          sshpass -e ssh -p $PORT $VPS "
            docker exec shared-nginx ls -la /etc/letsencrypt/live/lqd.bhquan.store/
            docker exec shared-nginx sh -c 'openssl x509 -in /etc/letsencrypt/live/lqd.bhquan.store/fullchain.pem -noout -subject -dates' 2>&1
            docker exec shared-nginx nginx -t 2>&1
            docker exec shared-nginx nginx -s reload 2>&1
          "
